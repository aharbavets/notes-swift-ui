import SwiftUI

struct NoteView: View {
    var body: some View {
        Text("Hello, world!")
            .padding()
    }
}

struct NoteView_Previews: PreviewProvider {
    static var previews: some View {
        NoteView()
    }
}
